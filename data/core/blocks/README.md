# Core Blocks

Blocks are JSON schema definitions stored in `data/core/blocks/`. They define the data shape of a content block — what fields it has, their types, labels, and options. They do NOT contain content. Content is stored in entries.

**Note:** `core/hero` has been moved to `data/blueprints/block/`. Blueprints are composed blocks, not primitives.
