/**
 * Field renderers index. Registers or configures CLI field type renderers.
 * Currently renderCliField implements a minimal default for all types.
 */
// No-op: renderCliField is the default implementation used by commands.
